@model IEnumerable<CCM3S_EIP.Models.VIEW_PO_PUBLIC_OBJECT_BOOKING_MEETINGROOM>
@{
    Layout = null;
}
<table class="table table-bordered table-striped">
    <tr>

        <th>
            會議室
        </th>
        <th>
            申請人
        </th>
        <th>
            用途說明
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @*@Html.DisplayFor(modelItem => item.ObjectNM)*@
                @Html.ActionLink(item.ObjectNM, "MeetRoomCalendar", "Home", new { Area = "" }, new { })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EMP_NM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject)
            </td>
        </tr>
    }

</table>